#include<iostream>
using namespace std;
int main() {
	int size;
	cout<<"Enter the size of array:";
	cin>>size;
	int arr[size];
	cout<<"Enter "<<size<<" Elements:\n";
	for(int i=0;i<size;i++){
		cin>>arr[i];
	}
	int x,count=0;
	cout<<"Enter x:";
	cin>>x;
	for(int i=0;i<size;i++){
	if(arr[i]>x){
		count++;
	}}
	cout<<"no of elements in array greater than x are:"<<count<<endl;
	/*int max=arr[0];
	int min=arr[0];
	for(int i=1;i<5;i++){
		if(arr[i]>max)
		max=arr[i];
		if(arr[i]<min)
		min=arr[i];
	}
	cout<<"Maximum number is:"<<max<<endl;
	cout<<"Minimum number is:"<<min<<endl;*/
	return 0;
}